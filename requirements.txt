# Core LangChain (0.2.x — prebuilt wheels; use Python 3.12 on Streamlit Cloud)
langchain>=0.2.16,<0.3
langchain-community>=0.2.16,<0.3
langchain-openai>=0.1.25,<0.2
langchain-anthropic>=0.1.23,<0.2
langchain-core>=0.2.43,<0.3

# Vector store (OpenAI embeddings only — no local torch/sentence-transformers)
chromadb>=0.5.5,<0.6

# API
fastapi>=0.109.0,<0.116
uvicorn[standard]>=0.27.0,<0.35
pydantic>=2.10.0,<3
pydantic-settings>=2.6.0,<3

# Streamlit UI
streamlit>=1.40.0,<2
streamlit-chat>=0.1.1

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# Monitoring & logging
loguru>=0.7.2
prometheus-client>=0.19.0

# Database (optional)
sqlalchemy>=2.0.25,<3
alembic>=1.13.1,<2

# Pin tokenizer with cp312/cp313 wheels (avoid Rust source builds on Cloud)
tiktoken>=0.8.0,<1
